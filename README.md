# Nishok Kumar S — Portfolio (React + Vite + Tailwind)

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```

2. Run locally:
   ```bash
   npm run dev
   ```

3. Deploy to GitHub Pages:
   ```bash
   npm run deploy
   ```

Certificates go in `/public/certs/`. Resume is already included in `/public/`.
